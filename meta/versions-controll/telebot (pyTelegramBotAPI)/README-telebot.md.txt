This version's maintanance *mainly* stopped before the 30 June, 2021.

Further version -- version with `telethon`.

Install dependencies: `pip install pyTelegramBotAPI` (run `pip install -r requirememts.txt` to install all).  Import via `import telebot`